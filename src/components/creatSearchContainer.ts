export const creatSearchContainer = () => {
// Skapa search-container
const searchContainer = document.createElement("div") as HTMLDivElement;
searchContainer.classList.add("search-container");

return searchContainer;
};